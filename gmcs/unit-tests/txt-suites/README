This directory contains the txt-suite files used in the unit tests.  You
should not move txt-suite files here by hand,.  However, when txt-suite
files are moved here by add-unit-test() (invoked in the lisp buffer
with the lkb running), you will need to cvs add them.
